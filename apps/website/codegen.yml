overwrite: true
schema:
  - ${GRAPHQL_ENDPOINT}:
      headers:
        x-hasura-admin-secret: ${HASURA_ADMIN_SECRET}
generates:
  app/api/create.ts:
    documents: app/pages/create/**/*.{ts,graphql}
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      withHooks: true
  app/api/admin.ts:
    documents: app/pages/admin/**/*.{ts,graphql}
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      withHooks: true
  app/api/participate.ts:
    documents: app/pages/participate/**/*.{ts,graphql}
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      withHooks: true
  src/pages/results/api.ts:
    documents: src/pages/results/**/*.{ts,graphql}
    plugins:
      - typescript
      - typescript-operations
    config:
      withHooks: true
